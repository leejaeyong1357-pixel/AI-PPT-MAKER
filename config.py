# -*- coding: utf-8 -*-
"""
변환 설정 파일.

실제 TECZEN ERP 외국어 업로드 '양식' 파일을 받으면, 보통 이 파일만 고치면 됩니다.
- COLUMNS: 업로드 엑셀의 컬럼 이름/순서 (실제 양식에 맞추세요)
- ABILITY_BY_GRADE / ABILITY_BY_SCORE: 등급·점수 -> 외국어 능력(상/중/하) 규칙
  ※ 아래 값은 '예시'입니다. 반드시 회사 기준으로 확인/수정하세요.
"""

# 1) ERP 업로드 엑셀의 컬럼 순서.
#    화면에서 본 외국어 표 컬럼에 사번/성명을 앞에 붙이고, 맨 뒤에 검토용 메모 컬럼을 둔 형태입니다.
#    실제 양식의 헤더 이름과 순서가 다르면 여기만 바꾸면 됩니다.
COLUMNS = [
    "사번",
    "성명",
    "외국어 구분",
    "외국어 시험",
    "유효일자",
    "듣기점수",
    "읽기점수",
    "쓰기점수",
    "말하기점수",
    "총점",
    "외국어 등급",
    "외국어 능력",
    "검증결과",  # 업로드 전에 삭제하세요. AI가 자동으로 채우는 '검토용' 메모 칸입니다.
]

# 2) 외국어 등급 -> 외국어 능력(상/중/하). (예시값 — 회사 기준으로 수정)
#    공백/대소문자는 무시하고 비교합니다. 예: "IM 3" -> "IM3"
ABILITY_BY_GRADE = {
    # OPIc
    "AL": "상", "IH": "상",
    "IM3": "중", "IM2": "중", "IM1": "중",
    "IL": "하", "NH": "하", "NM": "하", "NL": "하",
}

# 3) 등급이 없을 때 '시험 + 총점'으로 능력 결정. (예시값 — 회사 기준으로 수정)
#    각 리스트는 (기준점수, 능력) 이며 점수 내림차순으로 둡니다. total >= 기준점수 인 첫 항목 채택.
ABILITY_BY_SCORE = {
    "TOEIC": [(900, "상"), (700, "중"), (0, "하")],
    "TOEIC Speaking": [(160, "상"), (130, "중"), (0, "하")],
    "TEPS": [(452, "상"), (327, "중"), (0, "하")],
}
