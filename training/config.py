# -*- coding: utf-8 -*-
"""
교육 자동화 설정.

지금은 ①준비·검증 단계만 사용합니다. SELECTORS 등 RPA 항목은
실제 '교육결과등록' 화면 HTML을 받은 뒤 채웁니다.
"""

ERP_URL = "https://erp.teczen.kr"

# 수료증 폴더와 매칭 규칙
CERT_DIR = "수료증"            # 이 폴더에 '사번.pdf' 형태로 둔다 (예: 82211493.pdf)
CERT_EXTS = (".pdf", ".jpg", ".jpeg", ".png")
CERT_REQUIRED = True           # 이수여부 Y인 사람은 수료증 파일이 반드시 있어야 함

# 이수여부로 인정하는 값들(대소문자/공백 무시). 이 외에는 '미이수(N)'로 처리.
DONE_VALUES = {"y", "yes", "예", "이수", "완료", "o", "1", "true", "참석", "수료"}

# 작업목록 엑셀의 컬럼
COLUMNS = ["사번", "성명", "이수여부", "수료증파일", "상태"]

# 교육정보 시트에서 반드시 채워야 하는 항목
COURSE_REQUIRED = [
    "과정명", "사내외구분", "교육구분", "교육목적", "교육내용",
    "시작일", "종료일", "교육장소", "교육시간", "교육비",
]

# --- 아래는 ②RPA(브라우저 자동조작) 단계에서 채울 부분 (지금은 비워둠) ---
# 실제 교육결과등록 화면 HTML을 받아서 정확히 채울 예정.
SELECTORS = {
    # "row_by_sabun":   "...",  # 사번으로 해당 인원 행 찾기
    # "iban_checkbox":  "...",  # 이수여부 Y 체크/선택
    # "cert_upload":    "...",  # 수료증 파일 첨부 input
    # "save_button":    "...",  # 저장 버튼
}
